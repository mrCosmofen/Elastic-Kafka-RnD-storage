apply plugin: "war"

bootWar {
    mainClassName = "com.epam.elastic_kafka_rnd.storage.StorageApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
